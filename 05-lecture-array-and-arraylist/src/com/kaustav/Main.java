package com.kaustav;

import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        // Q: store a roll no
        int a = 60;

        // Q: store a person's name
        String n = "Kaustav";

        // Q: store 5 roll numbers using array
        // syntax
        // datatype[] variable_name = new datatype[size];
        int[] rnos = new int[5]; // by default all the elements are 0
        //or directly
        int [] rnos2 = {60, 61, 62, 63};

        // breakdown:
        // int rnos[]; ->delcaration
        // rnos = new int[5]; ->initialisation: actually here the object is created in the heap

    }
}
